<% if user.errors.any? %>
  <div class="alert alert-warning">
  	<%= user.errors.count.to_s+' errors prohibited this obj from being saved:' %>
    <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>